# Dependencies
node_modules/

# Next.js
.next/
out/

# Env files — never commit secrets
.env
.env.local
.env.*.local

# Capacitor : le projet natif Android est commite (config de signature,
# permissions du manifeste...) — seuls ses artefacts de build sont ignores,
# via android/.gitignore genere par `npx cap add android`.
apps/web/ios/
local.properties

# Signing keys — never commit
*.jks
*.keystore
keystore.properties

# Build output
dist/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
