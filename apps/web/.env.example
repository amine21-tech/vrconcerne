# Copiez ce fichier en .env.local pour le developpement.
# Au moment du build Capacitor (Phase 4), cette valeur doit pointer vers
# l'API de production en HTTPS : elle est figee dans le bundle statique.
NEXT_PUBLIC_API_URL=http://localhost:3101
